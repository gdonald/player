<div class="pt-2">
  <div class="row">
    <div class="col-6">
      <h5>Playlists</h5>
    </div>
    <div class="col-6 text-end">
      <%= link_to 'New', new_playlist_path, remote: true, class: 'btn btn-primary btn-sm' %>
    </div>
  </div>
  <table class="table table-striped table-hover table-bordered table-sm mt-2">
    <thead>
      <tr>
        <th class="px-2">Name</th>
        <th class="px-2 tight">MP3s</th>
        <th>&nbsp;</th>
      </tr>
    </thead>
    <tbody>
      <% playlists.each do |playlist| %>
        <tr class="align-middle">
          <td class="px-2"><%= playlist.name %></td>
          <td class="px-2"><%= playlist.playlist_mp3s.size %></td>
          <td class="px-2 tight">
            <%= link_to 'Edit', edit_playlist_path(playlist), remote: true, class: 'btn btn-primary btn-sm' %>
            <% if playlist.playlist_mp3s.size > 0 %>
              <%= link_to 'Play', play_playlist_path(playlist), remote: true, class: 'btn btn-primary btn-sm' %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
