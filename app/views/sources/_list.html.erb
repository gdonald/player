<div class="pt-2">
  <div class="row">
    <div class="col-6">
      <h5>Sources</h5>
    </div>
    <div class="col-6 text-end">
      <%= link_to 'New', new_source_path, remote: true, class: 'btn btn-primary btn-sm' %>
    </div>
  </div>
  <table class="table table-striped table-hover table-bordered table-sm mt-2">
    <thead>
      <tr>
        <th class="px-2">Path</th>
        <th class="px-2 tight">MP3s</th>
        <th>&nbsp;</th>
      </tr>
    </thead>
    <tbody>
      <% sources.each do |source| %>
        <tr class="align-middle">
          <td class="px-2"><%= source.path %></td>
          <td class="px-2"><%= source.mp3s.size %>
          <td class="tight">
            <%= link_to 'Edit', edit_source_path(source), remote: true, class: 'btn btn-primary btn-sm' %>
            <%= link_to 'Scan', sync_source_path(source), remote: true, class: 'btn btn-primary btn-sm' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
